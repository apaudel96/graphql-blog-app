[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
debug = "uvicorn app.app:app --reload"

[packages]
starlette = "*"
uvicorn = {extras = ["standard"], version = "*"}
strawberry-graphql = {extras = ["asgi"], version = "*"}
tortoise-orm = "*"
passlib = {extras = ["argon2"], version = "*"}

[dev-packages]

[requires]
python_version = "3.10"
